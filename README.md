# scoop-bucket

Scoop bucket repository.

This repository is intended to be managed by GoReleaser.
- Manifests live under `bucket/` (for example: `bucket/<name>.json`).

If you publish this repo to GitHub, GoReleaser can push updates here during releases.
